# Progress — 2026-09-03

## Summary

Work focused on verifying the research state, strengthening the executable
stage contracts, producing the canonical Stage 2 artifact, and identifying the
actual dependency that blocks official experiments. Implemented code and
executed empirical evidence remain explicitly separated.

## Stage status

| Stage | Status | Evidence and limitation |
|---:|---|---|
| 0 — Freeze and provenance | PASS | Repository audit, provenance and reproducibility infrastructure are present. PR #21 was merged. |
| 1 — WOMD data pipeline | `BLOCKED_NO_DATA` | The archived material contains no raw TFRecords, canonical NPZ corpus or compatible model checkpoints. Historical provenance reports 249,137 samples from 24,182 scenarios with 11 history and 80 future states, but the binary corpus is unavailable. The fail-closed preflight and tracking issue/PR were added. |
| 2 — PC-FMCW/DPSK link | ARTIFACT PASS; RAW-BER REVIEW REQUIRED | A 31-point LUT from -5 to 25 dB was generated with 250,000 decisions per point and seed 20260827. Provenance and the conservative monotone LUT are recorded. Statistically significant non-monotone raw-BER spikes require receiver-level diagnosis before this artifact is treated as fully validated physical-layer evidence. |
| 3 — Classical baselines | CODE READY; DIAGNOSTIC ONLY | Last Position, CV, CA, Kalman CV and IMM are implemented. A 56-trajectory archive diagnostic is not a canonical paper result. No official Stage 3 experiment was run without the canonical WOMD corpus. |
| 4 — Communication-aware GRU | DESIGN READY; NOT RUN | Four objectives, five paired seeds, 80 epochs, batch size 32 and development-only lambda selection define 20 canonical checkpoints. These final runs were not executed. Dataset availability precedes GPU availability as the current blocker. |
| 5 — Official predictor evaluation | NOT RUN | Requires the untouched official-validation corpus and frozen checkpoints. |
| 6 — Packet scheduling | NOT RUN | No official PDR, timely-goodput, latency or fairness results were generated. |
| 7 — Statistics and figures | NOT RUN | No canonical hypothesis tests or paper-level p-values were generated. |
| 8 — Final paper | NOT COMPLETE | Infrastructure exists, but the official empirical evidence chain is incomplete. |

## Canonical next sequence

1. Recover or rebuild the licensed canonical WOMD corpus.
2. Run the Stage 1 audit and freeze the split manifests.
3. Diagnose the Part-A raw-BER discontinuities and regenerate Stage 2 evidence.
4. Run Stage 3 canonical classical baselines.
5. Run Stage 4 lambda selection and train the 20 GRU checkpoints.
6. Execute Stages 5–7 on untouched official validation.
7. Build the Stage 8 manuscript only from frozen, traceable artifacts.

The immediate blocker is the unavailable canonical WOMD corpus, not GPU
capacity. No missing experiment has been replaced by synthetic or proxy claims.
